/* 47.	Write a program to find the range of a set of numbers.
 Range is the difference between the smallest 
 and biggest number in the list. */
 
#include <stdio.h>
#include <conio.h>
main()
{
	int a,range,lower,upper,i;
	printf("enter numbers(press crtl+f at end)");
	
	while(scanf("%d",&a))
	{
		/*if()
		lower=a;
		else
		lower=lower;
		
		if(upper<a)
		upper=a;
		else
		upper=upper;*/
		
	}
	printf("upper limit is %d and lower limit is %d",upper,lower);
	/*
	range=upper-lower;
	
	printf("range of entered numbers is %d",range);*/
	
	
	printf("\n\n\n\n Press any key to exit...");
	getch();
}

